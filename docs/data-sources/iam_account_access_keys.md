---
subcategory: "IAM (Identity & Access Management)"
page_title: "RadosGW: radosgw_iam_account_access_keys"
description: |-
  Lists the access keys of an IAM user within a RadosGW account (via the IAM ListAccessKeys API). Secrets are never returned. Use with account (or iam:ListAccessKeys-granted) credentials.
---

# radosgw_iam_account_access_keys

Lists the access keys of an IAM user within a RadosGW account (via the IAM `ListAccessKeys` API). Secrets are never returned. Use with account (or `iam:ListAccessKeys`-granted) credentials.

## Example Usage

```terraform
data "radosgw_iam_account_access_keys" "developer" {
  provider = radosgw.account_root
  user     = "developer"
}

output "developer_key_ids" {
  value = [for k in data.radosgw_iam_account_access_keys.developer.access_keys : k.access_key_id]
}
```

<!-- schema generated by tfplugindocs -->

## Argument Reference

The following arguments are supported:


* `user` - (Required) The IAM user whose access keys to list.




## Attributes Reference

The following attributes are exported:

* `access_keys` - The user's access keys. (see [below for nested schema](#nestedatt--access_keys))
* `id` - Data source identifier (the user name).
* `user` - See Argument Reference above.

<a id="nestedatt--access_keys"></a>
### Nested Schema for `access_keys`

Read-Only:

- `access_key_id` (String) The access key ID.
- `create_date` (String) The date and time the access key was created, in RFC 3339 format.
- `status` (String) The status of the access key: `Active` or `Inactive`.
